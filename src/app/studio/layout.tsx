export const metadata = {
    title: 'Growth Partner Studio',
    description: 'Sanity Embedded CMS Studio',
}

export default function StudioLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return children
}
